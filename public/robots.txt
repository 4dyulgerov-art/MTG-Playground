# TCG Playsim — robots.txt
# Allow all well-behaved crawlers full access. Single-page app; no
# admin paths or private routes are server-rendered, so nothing
# meaningful needs to be disallowed.
User-agent: *
Allow: /

# Sitemap
Sitemap: https://playsim.live/sitemap.xml

# Polite-crawl hint for aggressive crawlers
Crawl-delay: 1
